#include <stdio.h>
/**
 * main -This is the discription of the main function
 * Return: returns zero
 */
int main(void)
{
	int num = 0;

	while (num <= 10)
	{
		printf("%d", num);
		num++;
	}
putchar('\n');
return (0);
}
