#include <stdio.h>
/**
 * main -This is the discription for the main function
 * Return: 0(success)
 */
int main(void)
{
	int num;

	for (num = 48; num <= 57; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
