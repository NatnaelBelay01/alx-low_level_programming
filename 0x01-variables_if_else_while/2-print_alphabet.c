#include <stdio.h>
/**
 * main -this is the discription for the main function
 * Return: returns 0(success !)
 */
int main(void)
{
char a = 'a';

while (a <= 'z')
{
	putchar(a);
	a++;
}
putchar('\n');
return (0);
}
